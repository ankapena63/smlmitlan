[env:tasmota32]
extends = env:tasmota32_base

board = wt32-eth01

build_unflags =
  USE_SML

build_flags =
  -D USE_SML_M
  -D USE_BERRY
  -D USE_UFILESYS
  -D USE_ETHERNET
  -D USE_MQTT
  -D USE_WEBSERVER
``
